@{
    ViewData["Title"] = "Proxy Providers";
}

<div class="container-fluid">
    <div class="row mt-4">
        <div class="card col-12">
            <div class="card-body">
                <div class="text-center">
                    <h5 class="card-title">Proxy Test Server List</h5>
                </div>
                <div class="row mt-2 mb-2">
                    <button type="button" data-toggle="modal" data-remote="/proxytestserver/insert" data-target="#BaseModal" class="btn btn-primary btn-sm">New Test Server</button>
                </div>
                <table id="providerTable" class="table table-striped table-hover mt-2" style="width: 100%">
                    <thead class="thead-dark">
                        <tr>
                            <th>Name</th>
                            <th>Address</th>
                            <th></th>
                        </tr>
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts
{
    <script src="~/js/proxy.test.js"></script>
}

